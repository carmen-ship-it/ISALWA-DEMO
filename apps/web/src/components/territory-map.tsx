'use client';

/**
 * Public Territorio map entry — Mission 16 GIS foundation.
 * Preserves the previous export name so the page and tour selectors stay stable.
 */
export { TerritoryExperience as TerritoryMap } from './territorio/territory-experience';
