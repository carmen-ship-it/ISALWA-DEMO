import { CompanyHome } from "@/components/home/company-home";

/** Companies is the same surface as Home — living company memory list. */
export default function CompaniesPage() {
  return <CompanyHome />;
}
